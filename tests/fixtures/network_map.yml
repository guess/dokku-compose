networks:
  - backend-net
  - worker-net

apps:
  myapp:
    networks:
      - backend-net
    network:
      attach_post_create:
        - init-net
      initial_network: custom-bridge
      bind_all_interfaces: true
      tld: internal
