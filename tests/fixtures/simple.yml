dokku:
  plugins:
    postgres:
      url: https://github.com/dokku/dokku-postgres.git

networks:
  - app-net

apps:
  myapp:
    build_dir: apps/myapp
    ports:
      - "http:5000:5000"
    postgres: true
